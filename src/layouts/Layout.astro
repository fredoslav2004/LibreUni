---
import { BookOpen, Menu, X, Home, Book, Code2, Compass, Users } from 'lucide-react';
import Search from '../components/Search';
import ThemeToggle from '../components/ThemeToggle';
import { COLOR_MODES, DEFAULT_COLOR_MODE, DEFAULT_THEME_ID, THEMES } from '../utils/themes';
import 'katex/dist/katex.min.css';
import '../styles/global.css';

interface Props {
  title: string;
}

const { title } = Astro.props;
const themeIds = THEMES.map((theme) => theme.id);
const colorModes = COLOR_MODES.map((mode) => mode.id);
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta http-equiv="Cache-Control" content="no-cache, no-store, must-revalidate">
    <meta http-equiv="Pragma" content="no-cache">
    <meta http-equiv="Expires" content="0">
    <link rel="icon" type="image/svg+xml" href={`${import.meta.env.BASE_URL}favicon.svg`} />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Literata:opsz,wght@7..72,400;7..72,500;7..72,600;7..72,700;7..72,800&family=Plus+Jakarta+Sans:wght@300;400;500;600;700;800&family=Space+Grotesk:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <script is:inline src="https://cdn.jsdelivr.net/pyodide/v314.0.5/full/pyodide.js"></script>
    <script>
      // Force hoisted script generation
      console.log("LibreUni: Production bundle active");
    </script>
    <script is:inline define:vars={{ themeIds, colorModes, defaultThemeId: DEFAULT_THEME_ID, defaultColorMode: DEFAULT_COLOR_MODE }}>
      const storedTheme = typeof localStorage !== 'undefined' ? localStorage.getItem('theme') : null;
      const storedColorMode = typeof localStorage !== 'undefined' ? localStorage.getItem('color-mode') : null;
      const theme = themeIds.includes(storedTheme) ? storedTheme : defaultThemeId;
      const colorMode = colorModes.includes(storedColorMode) ? storedColorMode : defaultColorMode;
      const hour = new Date().getHours();
      const isDark = colorMode === 'dark' || (colorMode === 'auto' && (hour >= 22 || hour < 7));

      document.documentElement.dataset.theme = theme;
      document.documentElement.dataset.colorMode = colorMode;
      document.documentElement.classList.toggle('dark', isDark);
      window.localStorage.setItem('theme', theme);
      window.localStorage.setItem('color-mode', colorMode);
    </script>
  </head>
  <body class="bg-light-bg dark:bg-dark-bg min-h-screen font-sans text-light-text dark:text-dark-text antialiased scroll-smooth">
    <!-- Top Navigation Bar -->
    <header class="nav-fixed-header bg-light-bg dark:bg-dark-surface border-b border-light-border dark:border-dark-border flex items-center justify-between px-4 md:px-6">
      <div class="flex items-center gap-4 md:gap-8">
        <a href={import.meta.env.BASE_URL} class="flex min-h-11 items-center gap-2 md:gap-3 group">
            <div class="bg-primary p-1.5 md:p-2 rounded-lg md:rounded-xl shadow-lg shadow-primary/30 transform group-hover:rotate-6 transition-transform">
                <BookOpen size={18} className="text-white md:w-5 md:h-5" />
            </div>
            <span class="text-lg md:text-xl font-black tracking-tighter text-light-text dark:text-dark-text shrink-0">LibreUni</span>
        </a>
        <nav class="hidden md:flex items-center gap-2">
            <a href={import.meta.env.BASE_URL} class="min-h-11 px-4 py-2 hover:bg-light-accent/5 dark:hover:bg-dark-accent/5 rounded-xl text-sm font-bold transition-all">Home</a>
            <a href={`${import.meta.env.BASE_URL}courses`} class="min-h-11 px-4 py-2 hover:bg-light-accent/5 dark:hover:bg-dark-accent/5 rounded-xl text-sm font-bold transition-all">Courses</a>
            <a href={`${import.meta.env.BASE_URL}careers`} class="min-h-11 px-4 py-2 hover:bg-light-accent/5 dark:hover:bg-dark-accent/5 rounded-xl text-sm font-bold transition-all">Career Paths</a>
            <a href={`${import.meta.env.BASE_URL}development`} class="min-h-11 px-4 py-2 hover:bg-light-accent/5 dark:hover:bg-dark-accent/5 rounded-xl text-sm font-bold transition-all">Development</a>
            <a href={`${import.meta.env.BASE_URL}about`} class="min-h-11 px-4 py-2 hover:bg-light-accent/5 dark:hover:bg-dark-accent/5 rounded-xl text-sm font-bold transition-all">About</a>
        </nav>
      </div>

      <div class="flex items-center gap-2 md:gap-4 flex-1 justify-end">
         <div class="hidden md:block flex-1 max-w-md">
            <Search client:load baseUrl={import.meta.env.BASE_URL} />
         </div>
         <div class="flex items-center gap-2 md:gap-2">
            <ThemeToggle client:load />
            <button 
                id="mobile-menu-toggle"
                class="min-h-11 min-w-11 p-2 md:p-2.5 hover:bg-light-accent/10 dark:hover:bg-dark-accent/10 rounded-xl transition-colors md:hidden text-light-text dark:text-dark-text border border-light-border dark:border-dark-border"
                aria-label="Toggle Menu"
            >
                <span id="menu-icon"><Menu size={20} /></span>
                <span id="close-icon" class="hidden"><X size={20} /></span>
            </button>
         </div>
      </div>
    </header>

    <!-- Mobile Navigation Overlay -->
    <div id="mobile-menu" class="fixed inset-0 z-[90] bg-light-bg dark:bg-dark-bg transition-all duration-500 translate-x-full md:hidden pt-24 px-8 overflow-y-auto pointer-events-none">
        <div class="absolute inset-0 opacity-10 bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-light-accent dark:from-dark-accent via-transparent to-transparent"></div>
        
        <div class="relative z-10 flex flex-col gap-10">
            <div class="space-y-4">
                <p class="text-[10px] font-black text-light-muted dark:text-dark-muted uppercase tracking-[0.2em]">Navigation</p>
                <div class="grid grid-cols-1 gap-4">
                    <a href={import.meta.env.BASE_URL} class="flex items-center gap-4 p-6 bg-light-surface dark:bg-dark-surface border border-light-border dark:border-dark-border rounded-3xl group active:scale-95 transition-all">
                        <div class="p-3 bg-light-accent/5 dark:bg-dark-accent/5 rounded-2xl text-light-accent dark:text-dark-accent">
                            <Home size={24} />
                        </div>
                        <div class="flex flex-col">
                            <span class="text-xl font-black">Home</span>
                            <span class="text-xs font-bold text-light-muted dark:text-dark-muted">Return to dashboard</span>
                        </div>
                    </a>
                    <a href={`${import.meta.env.BASE_URL}courses`} class="flex items-center gap-4 p-6 bg-light-surface dark:bg-dark-surface border border-light-border dark:border-dark-border rounded-3xl group active:scale-95 transition-all">
                        <div class="p-3 bg-primary/10 rounded-2xl text-primary">
                            <Book size={24} />
                        </div>
                        <div class="flex flex-col">
                            <span class="text-xl font-black">Courses</span>
                            <span class="text-xs font-bold text-light-muted dark:text-dark-muted">Browse all courses</span>
                        </div>
                    </a>
                    <a href={`${import.meta.env.BASE_URL}careers`} class="flex items-center gap-4 p-6 bg-light-surface dark:bg-dark-surface border border-light-border dark:border-dark-border rounded-3xl group active:scale-95 transition-all">
                        <div class="p-3 bg-primary/10 rounded-2xl text-primary">
                            <Compass size={24} />
                        </div>
                        <div class="flex flex-col">
                            <span class="text-xl font-black">Career Paths</span>
                            <span class="text-xs font-bold text-light-muted dark:text-dark-muted">Follow role-based curricula</span>
                        </div>
                    </a>
                    <a href={`${import.meta.env.BASE_URL}about`} class="flex items-center gap-4 p-6 bg-light-surface dark:bg-dark-surface border border-light-border dark:border-dark-border rounded-3xl group active:scale-95 transition-all">
                        <div class="p-3 bg-primary/10 rounded-2xl text-primary">
                            <Users size={24} />
                        </div>
                        <div class="flex flex-col">
                            <span class="text-xl font-black">About</span>
                            <span class="text-xs font-bold text-light-muted dark:text-dark-muted">LibreUni's mission</span>
                        </div>
                    </a>
                    <a href={`${import.meta.env.BASE_URL}development`} class="flex items-center gap-4 p-6 bg-light-surface dark:bg-dark-surface border border-light-border dark:border-dark-border rounded-3xl group active:scale-95 transition-all">
                        <div class="p-3 bg-primary/10 rounded-2xl text-primary">
                            <Code2 size={24} />
                        </div>
                        <div class="flex flex-col">
                            <span class="text-xl font-black">Development</span>
                            <span class="text-xs font-bold text-light-muted dark:text-dark-muted">Quality reports and course metrics</span>
                        </div>
                    </a>
                </div>
            </div>

            <div class="space-y-4">
                <p class="text-[10px] font-black text-light-muted dark:text-dark-muted uppercase tracking-[0.2em]">Search Knowledge</p>
                <Search client:load baseUrl={import.meta.env.BASE_URL} />
            </div>

            <div class="mt-auto py-12 text-center">
                <p class="text-[10px] font-black text-light-muted dark:text-dark-muted uppercase tracking-[0.2em] opacity-40">© 2026 LIBREUNI PROJECT</p>
            </div>
        </div>
    </div>

    <!-- Mobile Sidebar Backdrop (for lessons) -->
    <div id="sidebar-backdrop" class="fixed inset-0 bg-black/60 backdrop-blur-sm z-[45] hidden transition-opacity opacity-0"></div>

    <div class="flex min-h-screen relative pt-16">
      <slot />
    </div>

    <div id="diagram-overlay" class="diagram-overlay" aria-hidden="true">
      <button type="button" class="diagram-overlay-close" aria-label="Close diagram">Close</button>
      <div class="diagram-overlay-panel" role="dialog" aria-modal="true" aria-label="Expanded diagram">
        <div id="diagram-overlay-content" class="diagram-overlay-content"></div>
      </div>
    </div>

    <script is:inline>
        const menuToggle = document.getElementById('mobile-menu-toggle');
        const mobileMenu = document.getElementById('mobile-menu');
        const menuIcon = document.getElementById('menu-icon');
        const closeIcon = document.getElementById('close-icon');

        menuToggle?.addEventListener('click', () => {
            const isOpen = !mobileMenu.classList.contains('translate-x-full');
            if (isOpen) {
                mobileMenu.classList.add('translate-x-full');
                mobileMenu.classList.add('pointer-events-none');
                menuIcon.classList.remove('hidden');
                closeIcon.classList.add('hidden');
                document.body.style.overflow = '';
            } else {
                mobileMenu.classList.remove('translate-x-full');
                mobileMenu.classList.remove('pointer-events-none');
                menuIcon.classList.add('hidden');
                closeIcon.classList.remove('hidden');
                document.body.style.overflow = 'hidden';
            }
        });

        // Close menu on link clicks
        mobileMenu?.querySelectorAll('a').forEach(link => {
            link.addEventListener('click', () => {
                mobileMenu.classList.add('translate-x-full');
                mobileMenu.classList.add('pointer-events-none');
                menuIcon.classList.remove('hidden');
                closeIcon.classList.add('hidden');
                document.body.style.overflow = '';
            });
        });

        const diagramOverlay = document.getElementById('diagram-overlay');
        const diagramOverlayContent = document.getElementById('diagram-overlay-content');
        const diagramOverlayClose = diagramOverlay?.querySelector('.diagram-overlay-close');
        let diagramPreviousFocus = null;

        function closeDiagramOverlay() {
            if (!diagramOverlay || !diagramOverlayContent) return;
            diagramOverlay.classList.remove('is-open');
            diagramOverlay.setAttribute('aria-hidden', 'true');
            diagramOverlayContent.replaceChildren();
            document.body.style.overflow = '';
            if (diagramPreviousFocus && typeof diagramPreviousFocus.focus === 'function') {
                diagramPreviousFocus.focus();
            }
        }

        document.addEventListener('click', (event) => {
            const target = event.target;
            if (!(target instanceof Element)) return;

            if (target.closest('.diagram-source')) return;

            const frame = target.closest('.diagram-frame');
            if (frame && !target.closest('a') && !target.closest('.diagram-overlay')) {
                const render = frame.querySelector('.diagram-render');
                if (!render || !diagramOverlay || !diagramOverlayContent) return;
                diagramPreviousFocus = document.activeElement;
                diagramOverlayContent.replaceChildren(render.cloneNode(true));
                diagramOverlay.classList.add('is-open');
                diagramOverlay.setAttribute('aria-hidden', 'false');
                document.body.style.overflow = 'hidden';
                diagramOverlayClose?.focus();
            }
        });

        diagramOverlayClose?.addEventListener('click', closeDiagramOverlay);
        diagramOverlay?.addEventListener('click', (event) => {
            if (event.target === diagramOverlay) closeDiagramOverlay();
        });
        document.addEventListener('keydown', (event) => {
            if (event.key === 'Escape' && diagramOverlay?.classList.contains('is-open')) {
                closeDiagramOverlay();
            }
        });

        // Copy button generic logic
        document.addEventListener('click', (event) => {
            const target = event.target;
            if (!(target instanceof Element)) return;

            const copyBtn = target.closest('.diagram-copy-button');
            if (copyBtn) {
                const code = copyBtn.getAttribute('data-code');
                if (code) {
                    navigator.clipboard.writeText(code);
                    const originalText = copyBtn.innerHTML;
                    copyBtn.innerHTML = 'Copied!';
                    setTimeout(() => copyBtn.innerHTML = originalText, 2000);
                }
                return;
            }

            // Zoom logic for rendered diagram overlay
            const diagramRender = target.closest('.diagram-overlay-content .diagram-render');
            if (diagramRender) {
                diagramRender.classList.toggle('zoomed');
            }
        });
    </script>

    <script>  const KEY = 'lu:dev-theme';
  const CATS = [
    { label:'Brand', vars:[{k:'--primary-rgb',l:'Primary'},{k:'--secondary-rgb',l:'Secondary'},{k:'--accent-rgb',l:'Accent'}] },
    { label:'Light', vars:[{k:'--light-bg-rgb',l:'BG'},{k:'--light-surface-rgb',l:'Surface'},{k:'--light-border-rgb',l:'Border'},{k:'--light-text-rgb',l:'Text'},{k:'--light-muted-rgb',l:'Muted'},{k:'--light-accent-rgb',l:'Accent'}] },
    { label:'Dark', vars:[{k:'--dark-bg-rgb',l:'BG'},{k:'--dark-surface-rgb',l:'Surface'},{k:'--dark-border-rgb',l:'Border'},{k:'--dark-text-rgb',l:'Text'},{k:'--dark-muted-rgb',l:'Muted'},{k:'--dark-accent-rgb',l:'Accent'}] },
  ];
  const FONTS = ['Plus Jakarta Sans','Literata','Space Grotesk'];

  function load() { try{return JSON.parse(localStorage.getItem(KEY))||{}}catch{return{}} }
  function read(k) {
    let o = load();
    if (o[k]) return o[k];
    return getComputedStyle(document.documentElement).getPropertyValue(k).trim();
  }
  function write(k, v) {
    let o = load();
    o[k] = v;
    localStorage.setItem(KEY, JSON.stringify(o));
    applyStyles();
  }
  function rgb2h(s) { let p=s.split(' ').map(Number); return '#'+p.map(v=>v.toString(16).padStart(2,'0')).join(''); }
  function h2r(h) { let m=/^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(h); return m?`${parseInt(m[1],16)} ${parseInt(m[2],16)} ${parseInt(m[3],16)}`:null; }

  function applyStyles() {
    let styleEl = document.getElementById('lu-dev-themer-styles');
    if (!styleEl) {
      styleEl = document.createElement('style');
      styleEl.id = 'lu-dev-themer-styles';
      document.head.appendChild(styleEl);
    }
    const o = load();
    const curT = document.documentElement.dataset.theme || 'monochrome';
    const lightVars = [];
    const darkVars = [];
    const globalVars = [];
    
    Object.keys(o).forEach(k => {
      const val = o[k];
      if (k === '--font-sans') {
        globalVars.push(`${k}: ${val};`);
      } else if (k.startsWith('--light-')) {
        lightVars.push(`${k}: ${val};`);
        if (k.endsWith('-rgb')) {
          try { lightVars.push(`${k.slice(0, -4)}: ${rgb2h(val)};`); } catch(e){}
        }
      } else if (k.startsWith('--dark-')) {
        darkVars.push(`${k}: ${val};`);
        if (k.endsWith('-rgb')) {
          try { darkVars.push(`${k.slice(0, -4)}: ${rgb2h(val)};`); } catch(e){}
        }
      } else {
        globalVars.push(`${k}: ${val};`);
        if (k.endsWith('-rgb')) {
          try { globalVars.push(`${k.slice(0, -4)}: ${rgb2h(val)};`); } catch(e){}
        }
      }
    });

    let css = '';
    const lightSelector = curT === 'modern' ? 'html[data-theme="modern"], :root' : `html[data-theme="${curT}"]`;
    const darkSelector = curT === 'modern' ? 'html.dark' : `html.dark[data-theme="${curT}"]`;
    const globalSelector = curT === 'modern' ? ':root' : `html[data-theme="${curT}"]`;

    if (globalVars.length) css += `${globalSelector} {\n  ${globalVars.join('\n  ')}\n}\n`;
    if (lightVars.length) css += `${lightSelector} {\n  ${lightVars.join('\n  ')}\n}\n`;
    if (darkVars.length) css += `${darkSelector} {\n  ${darkVars.join('\n  ')}\n}\n`;
    styleEl.textContent = css;
  }

  function restore() { applyStyles(); }

  function applyTheme(id) { document.documentElement.dataset.theme=id; localStorage.setItem('theme',id); applyStyles(); }
  function applyMode(id) { 
    document.documentElement.dataset.colorMode=id; 
    let h=new Date().getHours(); 
    let isDark=id==='dark'||(id==='auto'&&(h>=22||h<7)); 
    document.documentElement.classList.toggle('dark',isDark); 
    localStorage.setItem('color-mode',id);
    applyStyles();
  }

  function make() {
    let existing = document.getElementById('lu-dev-themer');
    if (existing) existing.remove();

    let c = document.createElement('div');
    c.id = 'lu-dev-themer';
    c.innerHTML = `
<style>
#lu-dev-themer{all:initial;font-family:system-ui,sans-serif;position:fixed;bottom:16px;right:16px;z-index:2147483647;font-size:13px;line-height:1.4;color:#111;--l-bg:#fff;--l-b2:#f5f5f5;--l-b3:#e5e5e5;--l-t:#111;--l-m:#666;--l-ac:#3b82f6;--br:8px}
#lut-btn{display:flex;align-items:center;justify-content:center;width:40px;height:40px;border-radius:999px;border:1px solid #e5e5e5;background:var(--l-bg);cursor:pointer;box-shadow:0 2px 12px rgba(0,0,0,.12);font-size:18px;transition:transform .15s;margin-left:auto}
#lut-btn:hover{transform:scale(1.08)}
#lut-btn.open{transform:rotate(45deg)}
#lut-panel{display:none;position:absolute;bottom:52px;right:0;width:340px;max-height:min(600px,80vh);overflow-y:auto;border:1px solid var(--l-b3);border-radius:var(--br);background:var(--l-bg);box-shadow:0 8px 32px rgba(0,0,0,.15);padding:0}
#lu-dev-themer.open #lut-panel{display:block}
#lu-dev-themer.open #lut-btn{transform:rotate(45deg)}
.lut-h{padding:12px 14px 0;font-size:11px;font-weight:700;text-transform:uppercase;letter-spacing:.08em;color:var(--l-m)}
.lut-row{display:flex;align-items:center;gap:8px;padding:4px 14px}
.lut-row.highlighted{background:rgba(59,130,246,.08) !important;border-left:3px solid var(--l-ac) !important;padding-left:11px !important}
.lut-row label{width:64px;font-size:11px;font-weight:600;color:var(--l-m);flex-shrink:0}
.lut-row input[type=color]{width:28px;height:28px;padding:0;border:1px solid var(--l-b3);border-radius:4px;cursor:pointer;background:none}
.lut-row input[type=color]::-webkit-color-swatch-wrapper{padding:0}
.lut-row input[type=color]::-webkit-color-swatch{border:none;border-radius:3px}
.lut-val{font-size:10px;font-family:monospace;color:var(--l-m)}
.lut-sep{margin:8px 14px;border:0;border-top:1px solid var(--l-b3)}
.lut-themes{display:flex;gap:4px;padding:8px 14px 4px}
.lut-themes button,.lut-modes button{flex:1;padding:6px 0;font-size:10px;font-weight:700;border:1px solid var(--l-b3);border-radius:6px;background:var(--l-b2);cursor:pointer;transition:all .1s;text-transform:uppercase;letter-spacing:.04em}
.lut-themes button.active,.lut-modes button.active{background:var(--l-ac);color:#fff;border-color:var(--l-ac)}
.lut-modes{display:flex;gap:4px;padding:4px 14px 8px}
.lut-actions{display:flex;gap:6px;padding:8px 14px 12px}
.lut-actions button{flex:1;padding:7px 0;font-size:10px;font-weight:700;border-radius:6px;cursor:pointer;border:1px solid var(--l-b3);background:var(--l-b2);transition:all .1s}
.lut-actions button:hover{background:var(--l-b3)}
.lut-actions .lut-reset{color:#ef4444;border-color:#fca5a5}
.lut-actions .lut-copy{border-color:var(--l-ac);color:var(--l-ac)}
.lut-input-select{flex:1;padding:4px 6px;font-size:11px;border:1px solid var(--l-b3);border-radius:4px;background:var(--l-bg);color:var(--l-t);outline:none}
.lut-input-select:focus{border-color:var(--l-ac)}
.lut-label{font-size:11px;font-weight:600;color:var(--l-t);flex:1}
.lut-btn-pick.active{background:var(--l-ac);color:#fff;border-color:var(--l-ac)}
.lut-btn-pick.picking{background:#f59e0b;color:#fff;border-color:#f59e0b;animation:lut-pulse 1s infinite}
@keyframes lut-pulse{0%,100%{opacity:1}50%{opacity:.6}}
.lut-elem{padding:6px 14px 12px;font-size:11px;line-height:1.5}
.lut-elem-tag{font-weight:700;font-family:monospace;font-size:12px;color:var(--l-t);word-break:break-all}
.lut-elem-meta{color:var(--l-m);font-size:10px;margin:2px 0}
.lut-elem-section{font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:.06em;color:var(--l-m);margin:6px 0 3px}
.lut-elem-var{font-family:monospace;font-size:10px;padding:1px 0;display:flex;gap:4px}
.lut-elem-var-k{color:#8b5cf6;flex-shrink:0}
.lut-elem-var-v{color:var(--l-t);word-break:break-all}
.lut-elem-empty{padding:12px 14px;font-size:10px;color:var(--l-m);text-align:center}
.lut-tabs{display:flex;gap:4px;padding:8px 14px 0;border-bottom:1px solid var(--l-b3)}
.lut-tab{flex:1;border:0;border-bottom:2px solid transparent;background:none;color:var(--l-m);cursor:pointer;font-size:10px;font-weight:800;padding:7px 2px;text-transform:uppercase;letter-spacing:.04em}
.lut-tab.active{border-color:var(--l-ac);color:var(--l-ac)}
.lut-view{display:none}.lut-view.active{display:block}
.lut-control{display:grid;grid-template-columns:1fr 92px;align-items:center;gap:8px;padding:4px 14px}
.lut-control label{font-size:11px;font-weight:650;color:var(--l-m)}
.lut-control input,.lut-request{width:100%;box-sizing:border-box;border:1px solid var(--l-b3);border-radius:5px;background:var(--l-b2);color:var(--l-t);font:11px monospace;padding:6px}
.lut-request{display:block;min-height:66px;margin:8px 14px;width:calc(100% - 28px);font-family:system-ui,sans-serif;resize:vertical}
.lut-small-actions{display:flex;gap:6px;padding:4px 14px 10px}.lut-small-actions button{flex:1}
.lut-note{margin:6px 14px;color:var(--l-m);font-size:10px;line-height:1.45}
.lut-context{white-space:pre-wrap;word-break:break-word;background:var(--l-b2);border:1px solid var(--l-b3);border-radius:5px;margin:8px 14px;padding:8px;font:10px/1.45 monospace;max-height:170px;overflow:auto}
.lut-topbar{display:flex;align-items:center;gap:6px;padding:12px 14px 8px;border-bottom:1px solid var(--l-b3)}
.lut-topbar-title{flex:1;font-size:12px;font-weight:850;color:var(--l-t)}
.lut-topbar button{padding:6px 8px;font-size:10px;font-weight:800;border:1px solid var(--l-b3);border-radius:6px;background:var(--l-b2);color:var(--l-t);cursor:pointer}
.lut-topbar button:hover{background:var(--l-b3)}
.lut-topbar #lut-pick{border-color:var(--l-ac);color:var(--l-ac)}
.lut-topbar #lut-pick.picking{background:#f59e0b;color:#fff;border-color:#f59e0b;animation:lut-pulse 1s infinite}
.lut-disclosure{border-bottom:1px solid var(--l-b3)}
.lut-disclosure summary{cursor:pointer;list-style:none;padding:10px 14px;font-size:11px;font-weight:800;color:var(--l-t)}
.lut-disclosure summary::-webkit-details-marker{display:none}
.lut-disclosure summary:before{content:'▸';display:inline-block;margin-right:7px;color:var(--l-ac);transition:transform .12s}
.lut-disclosure[open] summary:before{transform:rotate(90deg)}
</style>
<div id="lut-btn" aria-label="Open developer tools">+</div>
<div id="lut-panel">
  <div class="lut-topbar"><span class="lut-topbar-title">Developer tools</span><button id="lut-copy">Copy CSS</button><button class="lut-btn-pick" id="lut-pick">Pick</button><button class="lut-reset" id="lut-reset">Reset</button></div>
  <details class="lut-disclosure"><summary>Theme colors</summary><div id="lut-colors"></div></details>
  <details class="lut-disclosure"><summary>Typography</summary><div class="lut-row"><label>Font</label><select class="lut-input-select" id="lut-font"></select></div></details>
  <div class="lut-tabs" role="tablist"><button class="lut-tab active" data-lut-tab="inspect">Inspect</button><button class="lut-tab" data-lut-tab="experiment">Experiment</button><button class="lut-tab" data-lut-tab="handoff">Handoff</button></div>
  <div class="lut-view active" data-lut-view="inspect"><div id="lut-elem-info"><div class="lut-elem-empty">Pick an element to inspect it.</div></div></div>
  <div class="lut-view" data-lut-view="experiment"><div id="lut-experiment"><div class="lut-elem-empty">Pick an element, then try spacing or sizing changes here.</div></div></div>
  <div class="lut-view" data-lut-view="handoff"><p class="lut-note">Describe the durable change you want. The handoff includes the selected element, source hints, and staged CSS.</p><textarea id="lut-request" class="lut-request" placeholder="e.g. Reduce the card padding on mobile and keep the title aligned..."></textarea><div class="lut-small-actions"><button class="lut-copy" id="lut-copy-context">Copy context</button><button class="lut-copy" id="lut-copy-handoff">Copy handoff</button></div><div id="lut-context" class="lut-context">No element selected.</div></div>
</div>`;
    document.body.appendChild(c);
    document.getElementById('lut-btn').onclick = () => c.classList.toggle('open');

    let colorsEl = document.getElementById('lut-colors');
    CATS.forEach(cat => {
      let h = document.createElement('div');
      h.className = 'lut-h';
      h.textContent = cat.label;
      colorsEl.appendChild(h);
      cat.vars.forEach(v => {
        let val = read(v.k);
        let hex = val ? rgb2h(val) : '#888888';
        let row = document.createElement('div');
        row.className = 'lut-row';
        row.setAttribute('data-var', v.k);
        let inp = document.createElement('input');
        inp.type = 'color';
        inp.value = hex;
        inp.oninput = () => {
          let rgb = h2r(inp.value);
          if (rgb) { write(v.k, rgb); row.querySelector('.lut-val').textContent = rgb; }
        };
        let lbl = document.createElement('label');
        lbl.textContent = v.l;
        let valEl = document.createElement('span');
        valEl.className = 'lut-val';
        valEl.textContent = val;
        row.appendChild(inp); row.appendChild(lbl); row.appendChild(valEl);
        colorsEl.appendChild(row);
      });
    });

    let fontEl = document.getElementById('lut-font');
    let curFont = read('--font-sans');
    FONTS.forEach(f => {
      let o = document.createElement('option');
      o.value = f; o.textContent = f;
      if (f === curFont) o.selected = true;
      fontEl.appendChild(o);
    });
    fontEl.onchange = () => { write('--font-sans', fontEl.value); };

    // Clean up the MutationObserver when the themer panel is removed from the DOM
    const cleanupObserver = new MutationObserver((mutations) => {
      for (const mutation of mutations) {
        for (const removedNode of mutation.removedNodes) {
          if (removedNode === c) {
            cleanupObserver.disconnect();
            return;
          }
        }
      }
    });
    cleanupObserver.observe(document.body, { childList: true });

    restore();

    let picking = false;
    let pickOverlay = null;
    let elemInfoEl = document.getElementById('lut-elem-info');
    let experimentEl = document.getElementById('lut-experiment');
    let selectedEl = null;
    let stagedChanges = {};
    const esc = (value) => String(value ?? '').replace(/[&<>"']/g, (char) => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
    const cssProps = ['margin','padding','gap','font-size','line-height','border-radius','width'];

    document.querySelectorAll('[data-lut-tab]').forEach((tab) => tab.addEventListener('click', () => {
      const name = tab.dataset.lutTab;
      document.querySelectorAll('[data-lut-tab]').forEach((item) => item.classList.toggle('active', item === tab));
      document.querySelectorAll('[data-lut-view]').forEach((view) => view.classList.toggle('active', view.dataset.lutView === name));
    }));

    function stopPicking() {
      picking = false;
      document.getElementById('lut-pick').classList.remove('picking');
      if (pickOverlay) { pickOverlay.remove(); pickOverlay = null; }
      document.removeEventListener('mousemove', onPickMove);
      document.removeEventListener('click', onPickClick, true);
    }

    function onPickMove(e) {
      if (pickOverlay) {
        pickOverlay.style.display = 'none';
      }
      let el = document.elementFromPoint(e.clientX, e.clientY);
      if (pickOverlay) {
        pickOverlay.style.display = 'block';
      } else {
        pickOverlay = document.createElement('div');
        pickOverlay.style.cssText = 'position:fixed;pointer-events:none;z-index:2147483646;border:2px solid #3b82f6;background:rgba(59,130,246,.08);transition:all .08s;border-radius:2px';
        document.body.appendChild(pickOverlay);
      }
      if (el && !el.closest('#lu-dev-themer') && el !== document.documentElement && el !== document.body) {
        let r = el.getBoundingClientRect();
        pickOverlay.style.left = r.left+'px'; pickOverlay.style.top = r.top+'px';
        pickOverlay.style.width = r.width+'px'; pickOverlay.style.height = r.height+'px';
      }
    }

    function onPickClick(e) {
      let el = e.target;
      if (el === pickOverlay) {
        pickOverlay.style.display = 'none';
        el = document.elementFromPoint(e.clientX, e.clientY);
        pickOverlay.style.display = 'block';
      }
      if (el && el.closest('#lu-dev-themer')) return;
      e.preventDefault(); e.stopPropagation();
      stopPicking();
      showElemInfo(el);
    }

    function getUsedThemeVars(targetEl) {
      const used = new Set();
      const varMap = {
        '--primary': '--primary-rgb',
        '--primary-rgb': '--primary-rgb',
        '--secondary': '--secondary-rgb',
        '--secondary-rgb': '--secondary-rgb',
        '--accent': '--accent-rgb',
        '--accent-rgb': '--accent-rgb',
        '--light-bg': '--light-bg-rgb',
        '--light-bg-rgb': '--light-bg-rgb',
        '--light-surface': '--light-surface-rgb',
        '--light-surface-rgb': '--light-surface-rgb',
        '--light-border': '--light-border-rgb',
        '--light-border-rgb': '--light-border-rgb',
        '--light-text': '--light-text-rgb',
        '--light-text-rgb': '--light-text-rgb',
        '--light-muted': '--light-muted-rgb',
        '--light-muted-rgb': '--light-muted-rgb',
        '--light-accent': '--light-accent-rgb',
        '--light-accent-rgb': '--light-accent-rgb',
        '--dark-bg': '--dark-bg-rgb',
        '--dark-bg-rgb': '--dark-bg-rgb',
        '--dark-surface': '--dark-surface-rgb',
        '--dark-surface-rgb': '--dark-surface-rgb',
        '--dark-border': '--dark-border-rgb',
        '--dark-border-rgb': '--dark-border-rgb',
        '--dark-text': '--dark-text-rgb',
        '--dark-text-rgb': '--dark-text-rgb',
        '--dark-muted': '--dark-muted-rgb',
        '--dark-muted-rgb': '--dark-muted-rgb',
        '--dark-accent': '--dark-accent-rgb',
        '--dark-accent-rgb': '--dark-accent-rgb'
      };
      
      let curr = targetEl;
      while (curr && curr !== document.documentElement) {
        // 1. Check inline style
        const inline = curr.getAttribute('style') || '';
        for (const v in varMap) {
          if (inline.includes(v)) {
            used.add(varMap[v]);
          }
        }
        
        // 2. Check classes
        const classes = Array.from(curr.classList);
        for (const cls of classes) {
          if (cls.includes('primary')) used.add('--primary-rgb');
          if (cls.includes('secondary')) used.add('--secondary-rgb');
          if (cls.includes('accent')) used.add('--accent-rgb');
          if (cls.includes('light-bg') || cls.includes('bg-white')) used.add('--light-bg-rgb');
          if (cls.includes('light-surface') || cls.includes('bg-slate-50')) used.add('--light-surface-rgb');
          if (cls.includes('light-border') || cls.includes('border-slate-200')) used.add('--light-border-rgb');
          if (cls.includes('light-text') || cls.includes('text-slate-900')) used.add('--light-text-rgb');
          if (cls.includes('light-muted') || cls.includes('text-slate-500')) used.add('--light-muted-rgb');
          if (cls.includes('dark-bg')) used.add('--dark-bg-rgb');
          if (cls.includes('dark-surface')) used.add('--dark-surface-rgb');
          if (cls.includes('dark-border')) used.add('--dark-border-rgb');
          if (cls.includes('dark-text')) used.add('--dark-text-rgb');
          if (cls.includes('dark-muted')) used.add('--dark-muted-rgb');
        }

        // 3. Check matched stylesheets
        try {
          for (const sheet of document.styleSheets) {
            try {
              if (!sheet.cssRules) continue;
            } catch (e) {
              continue;
            }
            for (const rule of sheet.cssRules) {
              if (rule.type === CSSRule.STYLE_RULE && rule.selectorText) {
                try {
                  if (curr.matches(rule.selectorText)) {
                    const css = rule.style.cssText;
                    for (const v in varMap) {
                      if (css.includes(v)) {
                        used.add(varMap[v]);
                      }
                    }
                  }
                } catch (e) {}
              }
            }
          }
        } catch (e) {}
        
        curr = curr.parentElement;
      }
      return Array.from(used);
    }

    function showElemInfo(el) {
      if (!el || !elemInfoEl) return;
      selectedEl = el;
      let tag = el.tagName.toLowerCase();
      let id = el.id ? '#'+el.id : '';
      let cls = Array.from(el.classList).join('.');
      let sel = tag + (id || (cls ? '.'+cls : ''));
      let rect = el.getBoundingClientRect();
      let font = getComputedStyle(el).fontFamily.split(',')[0].replace(/['"]/g,'').trim();

      let cs = getComputedStyle(el);
      let vars = [];
      for (let i = 0; i < cs.length; i++) {
        let name = cs[i];
        if (name.startsWith('--')) {
          let val = cs.getPropertyValue(name).trim();
          if (val) vars.push({k:name,v:val});
        }
      }

      // Clear previous highlights and badges on the rows
      document.querySelectorAll('#lu-dev-themer .lut-row').forEach(row => {
        row.classList.remove('highlighted');
        const badge = row.querySelector('.lut-used-badge');
        if (badge) badge.remove();
      });

      // Highlight matching rows
      const usedVars = getUsedThemeVars(el);
      usedVars.forEach(vKey => {
        const row = document.querySelector(`#lu-dev-themer .lut-row[data-var="${vKey}"]`);
        if (row) {
          row.classList.add('highlighted');
          let lbl = row.querySelector('label');
          if (lbl && !row.querySelector('.lut-used-badge')) {
            let badge = document.createElement('span');
            badge.className = 'lut-used-badge';
            badge.textContent = 'used';
            badge.style.cssText = 'font-size:9px;font-weight:900;text-transform:uppercase;background:#3b82f6;color:#fff;padding:1px 4px;border-radius:3px;margin-left:4px;vertical-align:middle;display:inline-block';
            lbl.appendChild(badge);
          }
        }
      });

      let html = '<div class="lut-elem"><div class="lut-elem-tag">'+esc(sel)+'</div>';
      html += '<div class="lut-elem-meta">📐 '+Math.round(rect.width)+' × '+Math.round(rect.height)+'px</div>';
      html += '<div class="lut-elem-meta">📝 '+font+'</div>';
      html += '<div class="lut-elem-meta">📍 '+esc(el.closest('[data-lu-source]')?.getAttribute('data-lu-source') || 'Source mapping unavailable; use the DOM path below.')+'</div>';
      html += '<div class="lut-elem-meta">🧭 '+esc(domPath(el))+'</div>';
      if (vars.length) {
        html += '<div class="lut-elem-section">CSS Variables ('+vars.length+')</div>';
        vars.forEach(v => { html += '<div class="lut-elem-var"><span class="lut-elem-var-k">'+v.k+'</span><span class="lut-elem-var-v">'+v.v+'</span></div>'; });
      }
      html += '</div>';
      elemInfoEl.innerHTML = html;
      renderExperiment();
      renderContext();
    }

    function domPath(el) {
      const parts = [];
      let curr = el;
      while (curr && curr !== document.body && parts.length < 5) {
        let part = curr.tagName.toLowerCase();
        if (curr.id) part += '#'+curr.id;
        else if (curr.classList.length) part += '.'+Array.from(curr.classList).slice(0, 2).join('.');
        parts.unshift(part); curr = curr.parentElement;
      }
      return parts.join(' > ');
    }

    function experimentSelector(el) {
      if (!el.dataset.luDevId) el.dataset.luDevId = 'element-'+Math.random().toString(36).slice(2, 9);
      return `[data-lu-dev-id="${el.dataset.luDevId}"]`;
    }

    function applyExperiments() {
      let style = document.getElementById('lu-dev-experiment-styles');
      if (!style) { style = document.createElement('style'); style.id = 'lu-dev-experiment-styles'; document.head.appendChild(style); }
      const blocks = Object.entries(stagedChanges).map(([selector, changes]) => `${selector} { ${Object.entries(changes).filter(([, value]) => value).map(([prop, value]) => `${prop}: ${value} !important;`).join(' ')} }`);
      style.textContent = blocks.join('\n');
    }

    function renderExperiment() {
      if (!experimentEl || !selectedEl) return;
      const selector = experimentSelector(selectedEl);
      const changes = stagedChanges[selector] || {};
      experimentEl.innerHTML = '<p class="lut-note">These overrides are temporary and live only in this browser tab.</p><details class="lut-disclosure" open><summary>CSS properties</summary>' + cssProps.map((prop) => `<div class="lut-control"><label>${prop}</label><input data-lut-css="${prop}" value="${esc(changes[prop] || '')}" placeholder="${esc(getComputedStyle(selectedEl).getPropertyValue(prop))}"></div>`).join('') + '</details><div class="lut-small-actions"><button class="lut-copy" id="lut-clear-changes">Clear staged CSS</button></div>';
      experimentEl.querySelectorAll('[data-lut-css]').forEach((input) => input.addEventListener('input', () => {
        stagedChanges[selector] = stagedChanges[selector] || {};
        stagedChanges[selector][input.dataset.lutCss] = input.value.trim();
        applyExperiments(); renderContext();
      }));
      experimentEl.querySelector('#lut-clear-changes')?.addEventListener('click', () => { delete stagedChanges[selector]; applyExperiments(); renderExperiment(); renderContext(); });
    }

    function buildContext() {
      if (!selectedEl) return 'No element selected.';
      const cs = getComputedStyle(selectedEl);
      const selector = experimentSelector(selectedEl);
      const changes = stagedChanges[selector] || {};
      const component = selectedEl.closest('[data-component]')?.getAttribute('data-component') || (selectedEl.closest('article') ? 'lesson content component' : selectedEl.tagName.toLowerCase());
      const text = (selectedEl.textContent || '').replace(/\s+/g, ' ').trim().slice(0, 180);
      const local = selectedEl.closest('[data-lu-source]')?.getAttribute('data-lu-source') || 'Source mapping unavailable in this rendered build.';
      const general = `${component}; route ${location.pathname}; ${Math.round(selectedEl.getBoundingClientRect().width)}×${Math.round(selectedEl.getBoundingClientRect().height)}px; display ${cs.display}; ${domPath(selectedEl)}${text ? `; text: “${text}”` : ''}`;
      const css = Object.entries(changes).filter(([, value]) => value).map(([prop, value]) => `${prop}: ${value}`).join('; ') || 'none';
      return `LOCAL CONTEXT\n${local}\nDOM selector: ${selector}\nDOM path: ${domPath(selectedEl)}\n\nGENERAL CONTEXT\n${general}\n\nSTAGED VISUAL CHANGES\n${css}`;
    }

    function renderContext() { const contextEl = document.getElementById('lut-context'); if (contextEl) contextEl.textContent = buildContext(); }

    document.getElementById('lut-pick').onclick = () => {
      if (picking) { stopPicking(); return; }
      picking = true;
      document.getElementById('lut-pick').classList.add('picking');
      document.addEventListener('mousemove', onPickMove);
      document.addEventListener('click', onPickClick, true);
    };

    document.getElementById('lut-reset').onclick = () => {
      localStorage.removeItem(KEY);
      let styleEl = document.getElementById('lu-dev-themer-styles');
      if (styleEl) styleEl.remove();
      location.reload();
    };
    document.getElementById('lut-copy').onclick = () => {
      let curT = document.documentElement.dataset.theme;
      let curM = document.documentElement.dataset.colorMode;
      const isDark = curM === 'dark' || (curM === 'auto' && window.matchMedia('(prefers-color-scheme: dark)').matches);
      
      let selector = ':root';
      if (curT && curT !== 'modern') {
        selector = isDark ? `html.dark[data-theme='${curT}']` : `html[data-theme='${curT}']`;
      } else if (curT === 'modern') {
        selector = isDark ? 'html.dark' : 'html[data-theme=\'modern\']';
      }

      let lines = [`${selector} {`];
      CATS.forEach(c => c.vars.forEach(v => {
        let val = read(v.k);
        if (val) {
          if (v.k.endsWith('-rgb')) {
            const hexKey = v.k.slice(0, -4);
            try {
              const hexVal = rgb2h(val);
              lines.push(`  ${hexKey}: ${hexVal};`);
            } catch (e) {}
          }
          lines.push(`  ${v.k}: ${val};`);
        }
      }));
      let f = read('--font-sans');
      if (f) lines.push(`  --font-sans: ${f};`);
      lines.push('}');
      navigator.clipboard.writeText(lines.join('\n'));
    };
    document.getElementById('lut-copy-context')?.addEventListener('click', () => {
      navigator.clipboard.writeText(buildContext());
    });
    document.getElementById('lut-copy-handoff')?.addEventListener('click', () => {
      const request = document.getElementById('lut-request')?.value.trim() || 'Please review this element and suggest the appropriate permanent visual change.';
      navigator.clipboard.writeText(`${request}\n\n${buildContext()}\n\nIMPLEMENTATION NOTE\nUse the local source location when available. Translate staged CSS into the project’s existing classes or component styles, preserve responsive behavior, and remove any temporary browser-only override.`);
    });
    renderContext();
  }

  function init() {
    const isEnabled = localStorage.getItem('lu:enable-dev-themer') === 'true';
    if (isEnabled) {
      make();
    } else {
      let existing = document.getElementById('lu-dev-themer');
      if (existing) existing.remove();
    }
  }

  restore();

  window.addEventListener('lu:toggle-dev-themer', init);
  if (document.readyState === 'loading') document.addEventListener('DOMContentLoaded', init);
  else init();
    </script>
  </body>
</html>

<style is:global>
  @reference '../styles/global.css';
  :root {
    --accent: 0, 0, 0;
  }
  .dark {
    --accent: 255, 255, 255;
  }

  body {
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    margin: 0;
  }

  .nav-fixed-header {
    position: fixed !important;
    top: 0 !important;
    left: 0 !important;
    right: 0 !important;
    height: 4rem !important;
    z-index: 50 !important;
    width: 100% !important;
  }

  /* KATEX VISUAL FIXES */
  .katex-display {
    margin: 2rem 0 !important;
    overflow-x: auto;
    overflow-y: hidden;
    width: 100%;
  }

  .katex {
    font-size: 1.1em !important;
    text-indent: 0 !important;
  }

  .dark .katex {
    color: #ededed !important;
  }

  /* UNIFIED CODE BLOCK SYSTEM (START FROM ZERO) */
  .astro-code {
    @apply rounded-[1.5rem] p-6 my-8 shadow-sm border transition-colors duration-200;
    font-size: 15.2px !important;
    line-height: 25.84px !important;
    background-color: var(--astro-code-color-background) !important;
    border-color: var(--light-border) !important;
    color: var(--astro-code-color-text) !important;
  }

  html.dark .astro-code {
    border-color: var(--dark-border) !important;
  }

  /* Syntax Classes for Interactive Highlighting (Refined Academic Palette) */
  /* These will be used by our custom components to ensure visual parity */
  
  /* DEFAULT TEXT - High Contrast */
  .hl-default { color: var(--light-text); }
  html.dark .hl-default { color: var(--dark-text); }
  /* KEYWORDS - Sophisticated Rose/Pink */
  .hl-keyword { font-weight: bold; color: #BE123C; }
  html.dark .hl-keyword { color: #FB7185; }
  /* TYPES - Deep Teal/Cyan */
  .hl-type { font-weight: bold; color: #0369A1; }
  html.dark .hl-type { color: #38BDF8; }
  /* FUNCTIONS - Academic Blue */
  .hl-function { color: #1D4ED8; }
  html.dark .hl-function { color: #60A5FA; }
  /* STRINGS - Forest Green/Emerald */
  .hl-string { color: #15803D; }
  html.dark .hl-string { color: #34D399; }
  /* COMMENTS - Neutral Gray */
  .hl-comment { font-style: italic; color: #475569; }
  html.dark .hl-comment { color: #94A3B8; }
  /* OPERATORS - Dark Neutral */
  .hl-operator { color: #1E293B; }
  html.dark .hl-operator { color: #E2E8F0; }
  /* DIRECTIVES - Royal Purple */
  .hl-directive { color: #6D28D9; }
  html.dark .hl-directive { color: #A78BFA; }
  /* NUMBERS - Burnt Orange */
  .hl-number { color: #B45309; }
  html.dark .hl-number { color: #FBBF24; }
  /* BRACKETS - Muted Slate */
  .hl-bracket { color: #64748b; }
  html.dark .hl-bracket { color: #94a3b8; }

  /* Code Container Base */
  .code-container-base {
    @apply font-mono rounded-3xl border transition-all;
    @apply bg-white dark:bg-[#0f172a] border-light-border dark:border-slate-800;
    @apply text-light-text dark:text-dark-text;
    padding: 40px; /* p-10 */
    font-size: 15.2px;
    line-height: 25.84px;
  }

  .diagram-frame {
    position: relative;
    width: 100%;
    margin-left: auto;
    margin-right: auto;
    max-width: 100%;
  }

  .diagram-render {
    overflow: hidden;
  }

  .diagram-render > svg,
  .diagram-render img {
    display: block;
    cursor: pointer;
    /* Use the available reading width so labels remain legible at a glance. */
    width: 100% !important;
    height: auto !important;
    max-width: 100% !important;
    max-height: min(64vh, 720px);
    object-fit: contain;
    transition: transform 0.25s ease-out;
  }

  .diagram-toolbar {
    position: absolute;
    top: 0.875rem;
    right: 0.875rem;
    z-index: 3;
    display: flex;
    align-items: flex-start;
    gap: 0.5rem;
  }

  .diagram-source {
    position: relative;
  }

  .diagram-source-summary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 2rem;
    padding: 0 0.75rem;
    border-radius: 999px;
    border: 1px solid var(--light-border);
    background: rgba(255, 255, 255, 0.92);
    color: var(--light-text);
    font-size: 0.65rem;
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    list-style: none;
    cursor: pointer;
    opacity: 0;
    transform: translateY(-0.25rem);
    transition: opacity 0.2s, transform 0.2s, background-color 0.2s;
    box-shadow: 0 10px 25px rgba(15, 23, 42, 0.12);
  }

  .diagram-frame:hover .diagram-source-summary,
  .diagram-source-summary:focus-visible,
  .diagram-source[open] .diagram-source-summary {
    opacity: 1;
    transform: translateY(0);
  }

  .diagram-source-summary::-webkit-details-marker {
    display: none;
  }

  .dark .diagram-source-summary {
    border-color: var(--dark-border);
    background: rgba(15, 23, 42, 0.92);
    color: var(--dark-text);
  }

  .diagram-source-code {
    position: absolute;
    top: calc(100% + 0.625rem);
    right: 0;
    width: min(42rem, calc(100vw - 3rem));
    border-radius: 1rem !important;
    border: 1px solid var(--light-border);
    background: rgba(255, 255, 255, 0.98) !important;
    color: var(--light-text) !important;
    font-size: 0.78rem !important;
    line-height: 1.55 !important;
    text-align: left;
    box-shadow: 0 24px 60px rgba(15, 23, 42, 0.2);
    cursor: auto;
    z-index: 50;
  }

  .dark .diagram-source-code {
    border-color: var(--dark-border);
    background: rgba(15, 23, 42, 0.98) !important;
    color: var(--dark-text) !important;
  }

  .diagram-source-title {
    align-self: flex-start;
    margin-top: 0.75rem;
    font-size: 0.62rem;
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: 0.18em;
    color: var(--light-muted);
  }

  .dark .diagram-source-title {
    color: var(--dark-muted);
  }

  .diagram-overlay {
    position: fixed;
    inset: 0;
    z-index: 100000;
    display: none;
    align-items: center;
    justify-content: center;
    padding: 5rem 1rem 1rem;
    background: rgba(2, 6, 23, 0.72);
    backdrop-filter: blur(14px);
  }

  .diagram-overlay.is-open {
    display: flex;
  }

  .diagram-overlay-panel {
    max-width: 96vw;
    max-height: 84vh;
    overflow: auto;
    border-radius: 1.25rem;
    border: 1px solid rgba(226, 232, 240, 0.35);
    background: #ffffff;
    padding: 1.25rem;
    box-shadow: 0 30px 100px rgba(0, 0, 0, 0.35);
  }

  .dark .diagram-overlay-panel {
    background: #0f172a;
    border-color: rgba(148, 163, 184, 0.24);
  }

  .diagram-overlay-content .diagram-render {
    max-width: none;
    overflow: visible;
    cursor: zoom-in;
  }

  .diagram-overlay-content .diagram-render.zoomed {
    cursor: zoom-out;
  }

  .diagram-overlay-content .diagram-render > svg,
  .diagram-overlay-content .diagram-render img {
    width: 100% !important;
    height: 84vh !important;
    max-width: 100% !important;
    max-height: 84vh !important;
    object-fit: contain;
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .diagram-overlay-content .diagram-render.zoomed > svg,
  .diagram-overlay-content .diagram-render.zoomed img {
    width: min(2000px, 150vw) !important;
    height: auto !important;
    max-height: none !important;
  }

  .diagram-overlay-close {
    position: fixed;
    top: 1rem;
    right: 1rem;
    z-index: 1;
    min-height: 2.5rem;
    padding: 0 1rem;
    border-radius: 999px;
    border: 1px solid rgba(255, 255, 255, 0.22);
    background: rgba(15, 23, 42, 0.92);
    color: white;
    font-size: 0.72rem;
    font-weight: 900;
    text-transform: uppercase;
    letter-spacing: 0.14em;
    box-shadow: 0 16px 40px rgba(0, 0, 0, 0.28);
  }

  ::-webkit-scrollbar {
    width: 10px;
  }
  ::-webkit-scrollbar-track {
    @apply bg-transparent;
  }
  ::-webkit-scrollbar-thumb {
    @apply bg-slate-300 dark:bg-slate-700 rounded-full border-4 border-transparent bg-clip-padding;
  }
  ::-webkit-scrollbar-thumb:hover {
    @apply bg-slate-400 dark:bg-slate-600;
  }
</style>
